#ifndef OBSTACLE_H
#define OBSTACLE_H

class Obstacle
{
    double xPos;
    double yPos;

public:
    Obstacle(double initX, double initY)
    {
        xPos = initX;
        yPos = initY;
    }
};


#endif // OBSTACLE_H
